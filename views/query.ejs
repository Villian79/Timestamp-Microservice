<!DOCTYPE html>
<html lang='en'>
    <head>
        <meta charset='utf-8'>
        <title>Unix Timestamp</title>
        <link rel='stylesheet' type='text/css' href='/main.css'>
    </head>
    <body>
        <h1>Freecodecamp API Projects: Timestamp Microservice</h1>
        <p>This project implements following User Stories:</p>
        <ul>
            <li>I can pass a string as a parameter, and it will check to see whether that string contains either a unix timestamp
            or a natural language date (example: January 1, 2016).</li>
            <li>If it does, it returns both the Unix timestamp and the natural language form of that date.</li>
            <li>If it does not contain a date or Unix timestamp, it returns null for those properties.</li>
        </ul>
        <h3>Usage Examples:</h3>
        <code>https://boiling-dawn-87851.herokuapp.com/July%2010%202017</code>
        <br>
        <p><em>---You can use any date format, e.i 'July 10 2017', '10 July 2017', '2017 July 10' etc.</em></p>
        <br>
        <code>https://boiling-dawn-87851.herokuapp.com/1499644800</code>
        <br>
        <h3>Result expanation:</h3>
        <p>Once you complete your query in the command line and submit it, you will be shown the result of your submition</p>
    </body>
</html>